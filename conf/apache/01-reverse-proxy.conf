# Apache routing for the four public PathoCore deployment endpoints.
# The installer renders this source with the protected deployment environment.

<VirtualHost *:8080>
    ServerName ${APACHE_SERVER_NAME}
    ProxyRequests Off
    ProxyPreserveHost On
    AllowEncodedSlashes NoDecode
    RequestHeader set X-Forwarded-Proto "${APACHE_FORWARDED_PROTO}"
    RequestHeader set X-Forwarded-Port "${APACHE_FORWARDED_PORT}"
    RequestHeader set X-Forwarded-Host "${APACHE_SERVER_NAME}"
    LimitRequestBody ${APACHE_LIMIT_REQUEST_BODY}
    <IfModule security2_module>
        SecRequestBodyLimit ${APACHE_LIMIT_REQUEST_BODY}
        SecRequestBodyNoFilesLimit ${APACHE_LIMIT_REQUEST_BODY}
    </IfModule>
    ProxyPass / http://pathocore_web:${PATHOCORE_WEB_APP_PORT}/
    ProxyPassReverse / http://pathocore_web:${PATHOCORE_WEB_APP_PORT}/
    ProxyTimeout ${APACHE_PROXY_TIMEOUT}
    TimeOut ${APACHE_PROXY_TIMEOUT}
    CustomLog logs/${APACHE_LOG_STEM}-apache.access.log combined env=!forwarded
    CustomLog logs/${APACHE_LOG_STEM}-apache.access.log proxy env=forwarded
    ErrorLog logs/${APACHE_LOG_STEM}-apache.error.log
</VirtualHost>

<VirtualHost *:8080>
    ServerName ${APACHE_PATHOCORE_API_SERVER_NAME}
    ProxyRequests Off
    ProxyPreserveHost On
    AllowEncodedSlashes NoDecode
    RequestHeader set X-Forwarded-Proto "${APACHE_FORWARDED_PROTO}"
    RequestHeader set X-Forwarded-Port "${APACHE_FORWARDED_PORT}"
    RequestHeader set X-Forwarded-Host "${APACHE_PATHOCORE_API_SERVER_NAME}"
    LimitRequestBody ${APACHE_LIMIT_REQUEST_BODY}
    <IfModule security2_module>
        SecRequestBodyLimit ${APACHE_LIMIT_REQUEST_BODY}
        SecRequestBodyNoFilesLimit ${APACHE_LIMIT_REQUEST_BODY}
    </IfModule>
    ProxyPass /static/ !
    Alias /static/ "${PATHOCORE_API_INSTALL_PATH}/static/"
    <Directory "${PATHOCORE_API_INSTALL_PATH}/static">
        Require all granted
    </Directory>
    ProxyPass /documents/ !
    Alias /documents/ "${PATHOCORE_API_INSTALL_PATH}/documents/"
    <Directory "${PATHOCORE_API_INSTALL_PATH}/documents">
        Require all granted
    </Directory>
    ProxyPass / http://pathocore_api:${PATHOCORE_API_APP_PORT}/
    ProxyPassReverse / http://pathocore_api:${PATHOCORE_API_APP_PORT}/
    ProxyTimeout ${APACHE_PROXY_TIMEOUT}
    TimeOut ${APACHE_PROXY_TIMEOUT}
    CustomLog logs/pathocore-api-apache.access.log combined env=!forwarded
    CustomLog logs/pathocore-api-apache.access.log proxy env=forwarded
    ErrorLog logs/pathocore-api-apache.error.log
</VirtualHost>

<VirtualHost *:8080>
    ServerName ${APACHE_MEPRAM_OMOP_API_SERVER_NAME}
    ProxyRequests Off
    ProxyPreserveHost On
    AllowEncodedSlashes NoDecode
    RequestHeader set X-Forwarded-Proto "${APACHE_FORWARDED_PROTO}"
    RequestHeader set X-Forwarded-Port "${APACHE_FORWARDED_PORT}"
    RequestHeader set X-Forwarded-Host "${APACHE_MEPRAM_OMOP_API_SERVER_NAME}"
    LimitRequestBody ${APACHE_LIMIT_REQUEST_BODY}
    <IfModule security2_module>
        SecRequestBodyLimit ${APACHE_LIMIT_REQUEST_BODY}
        SecRequestBodyNoFilesLimit ${APACHE_LIMIT_REQUEST_BODY}
    </IfModule>
    ProxyPass /static/ !
    Alias /static/ "${MEPRAM_OMOP_API_INSTALL_PATH}/static/"
    <Directory "${MEPRAM_OMOP_API_INSTALL_PATH}/static">
        Require all granted
    </Directory>
    ProxyPass /documents/ !
    Alias /documents/ "${MEPRAM_OMOP_API_INSTALL_PATH}/documents/"
    <Directory "${MEPRAM_OMOP_API_INSTALL_PATH}/documents">
        Require all granted
    </Directory>
    ProxyPass / http://mepram_omop_api:${MEPRAM_OMOP_API_APP_PORT}/
    ProxyPassReverse / http://mepram_omop_api:${MEPRAM_OMOP_API_APP_PORT}/
    ProxyTimeout ${APACHE_PROXY_TIMEOUT}
    TimeOut ${APACHE_PROXY_TIMEOUT}
    CustomLog logs/mepram-omop-api-apache.access.log combined env=!forwarded
    CustomLog logs/mepram-omop-api-apache.access.log proxy env=forwarded
    ErrorLog logs/mepram-omop-api-apache.error.log
</VirtualHost>

<VirtualHost *:8080>
    ServerName ${APACHE_KEYCLOAK_SERVER_NAME}
    ProxyRequests Off
    ProxyPreserveHost On
    AllowEncodedSlashes NoDecode
    RequestHeader set X-Forwarded-Proto "${APACHE_FORWARDED_PROTO}"
    RequestHeader set X-Forwarded-Port "${APACHE_FORWARDED_PORT}"
    RequestHeader set X-Forwarded-Host "${APACHE_KEYCLOAK_SERVER_NAME}"
    LimitRequestBody ${APACHE_LIMIT_REQUEST_BODY}
    <IfModule security2_module>
        SecRequestBodyLimit ${APACHE_LIMIT_REQUEST_BODY}
        SecRequestBodyNoFilesLimit ${APACHE_LIMIT_REQUEST_BODY}
    </IfModule>
    ProxyPass / http://keycloak:8080/
    ProxyPassReverse / http://keycloak:8080/
    ProxyTimeout ${APACHE_PROXY_TIMEOUT}
    TimeOut ${APACHE_PROXY_TIMEOUT}
    CustomLog logs/keycloak-apache.access.log combined env=!forwarded
    CustomLog logs/keycloak-apache.access.log proxy env=forwarded
    ErrorLog logs/keycloak-apache.error.log
</VirtualHost>

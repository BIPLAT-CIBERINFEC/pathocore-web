# Optional Apache server-status config for PathoCore services.

ExtendedStatus On

<VirtualHost *:8080>
    ServerName ${SERVER_STATUS_SERVER_NAME}

    <Location /server-status>
        SetHandler server-status
        Require ip ${SERVER_STATUS_ALLOW_FROM}
    </Location>
</VirtualHost>

import { TitleOnlyPage } from "@/components/mepram/TitleOnlyPage";

export default function RedlabraPage() {
  return (
    <TitleOnlyPage
      title="Redlabra"
      breadcrumbs={[{ label: "Projects" }, { label: "Redlabra" }]}
    />
  );
}
